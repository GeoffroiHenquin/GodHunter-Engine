#pragma once

namespace GodHunter {

	namespace UserEvents {

		// Add or remove event (decide that jump = keydown_spacebar)

		//void DeclareUserEvent(char* _eventName);
		//void DeclareUserEvent(const char* _eventName);
		//void DestroyUserEvent(char* _eventName);
		//void DestroyUserEvent(const char* _eventName);
		
		// Declare event was called

		//void CallUserEvent();

		// Check if event was called

		//bool UserEvent(char* _eventName);
		//bool UserEvent(const char* _eventName);

	}

}